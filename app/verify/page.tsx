import { InnerPage } from "@/components/inner-page";
export default function Verify(){return <InnerPage eyebrow="PUBLIC RECORD LOOKUP" title="Verify a registry record" intro="Enter a registry ID or certificate number. Only privacy-approved record information will be displayed."><form className="verify-form"><label htmlFor="registry-id">Registry ID or certificate number</label><div><input id="registry-id" name="registry-id" placeholder="RPR-OLY-A12-0001847" pattern="[A-Za-z0-9-]+"/><button type="submit">VERIFY RECORD ↗</button></div><small>Verification search is a Phase 3 preview. No customer records are currently stored.</small></form></InnerPage>}
