import type { Metadata } from "next";
import { Header } from "@/components/header"; import { Footer } from "@/components/footer"; import { MarsMap } from "@/components/mars/mars-map"; import { RegionCard } from "@/components/mars/region-card"; import { marsRegions } from "@/data/mars-regions";
export const metadata:Metadata={title:"Explore Mars",description:"Explore five Mars-inspired registry collections and choose a symbolic commemorative designation."};
export default function ExplorePage(){return <main className="explore-page"><section className="explore-hero"><Header/><div className="shell"><p className="eyebrow">FIVE COLLECTIONS · ONE RED PLANET</p><h1>EXPLORE<br/><em>THE RED PLANET</em></h1><p>Discover remarkable Martian regions and choose a symbolic registry designation associated with memorable coordinates.</p></div></section><section className="map-section section shell"><div className="section-heading"><div><p className="eyebrow">CARTOGRAPHIC EXPLORER</p><h2>Choose your <em>horizon.</em></h2></div><p className="section-note">Tap a marker to explore. The map is an original visual interpretation and reference coordinates identify the associated collection area.</p></div><MarsMap/><p className="flow-disclosure">This is a symbolic/commemorative designation in a privately maintained registry and does not constitute legally recognized ownership of Martian real estate.</p></section><section className="collection-section section"><div className="shell"><p className="eyebrow">REGION COLLECTIONS</p><div className="explore-grid">{marsRegions.map((region,index)=><RegionCard key={region.slug} region={region} index={index}/>)}</div></div></section><Footer/></main>}
