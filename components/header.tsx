"use client";
import Link from "next/link";
import { useState } from "react";
const links = [["Explore Mars", "/#regions"], ["Gift a Plot", "/#packages"], ["How It Works", "/#how-it-works"], ["Registry", "/verify"], ["Verify", "/verify"], ["About", "/about"]];
export function Header() { const [open, setOpen] = useState(false); return <header className="site-header shell"><Link className="logo" href="/" aria-label="Red Planet Registry home"><span className="logo-orbit">R</span><strong>RED PLANET<br />REGISTRY</strong></Link><nav className="desktop-nav" aria-label="Main navigation">{links.map(([label, href]) => <Link href={href} key={label}>{label}</Link>)}</nav><div className="header-actions"><Link className="verify-link" href="/verify" aria-label="Verify registry">⌕</Link><Link className="nav-cta" href="/#regions">EXPLORE MARS ↗</Link><button className="menu-button" onClick={() => setOpen(!open)} aria-expanded={open} aria-controls="mobile-nav" aria-label="Toggle navigation"><span /><span /></button></div><nav id="mobile-nav" className={`mobile-nav ${open ? "open" : ""}`} aria-label="Mobile navigation">{links.map(([label, href]) => <Link href={href} onClick={() => setOpen(false)} key={label}>{label}<span>↗</span></Link>)}<p>Symbolic places. Personal stories.<br />Preserved on the Red Planet.</p></nav></header>; }
